(defproject config "0.1.0-SNAPSHOT"
  :description "Helps to configure a library if it follows the convention that the library functions get the configuration as first parameter."
  :dependencies [[org.clojure/clojure "1.2.1"]]
  :dev-dependencies [[com.stuartsierra/lazytest "1.1.2"]
                     [swank-clojure "1.4.0-SNAPSHOT"]])
